name: Deploy to Server (Development)

on:
  push:
    branches:
      - development

jobs:
  deploy:
    name: SSH Deploy & Docker
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up SSH Agent for VPS
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SERVER_SSH_KEY }}

      - name: Upload GitHub Repo Deploy Key to Server
        run: |
          echo "${{ secrets.REPO_SSH_KEY }}" > id_rsa_repo
          chmod 600 id_rsa_repo
          scp -o StrictHostKeyChecking=no id_rsa_repo ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:~/.ssh/id_rsa_repo

      - name: Configure SSH GitHub Access on Server
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} << 'EOSSH'
            mkdir -p ~/.ssh
            chmod 700 ~/.ssh

            echo -e "Host github.com\n  IdentityFile ~/.ssh/id_rsa_repo\n  IdentitiesOnly yes" > ~/.ssh/config
            chmod 600 ~/.ssh/config
          EOSSH

      - name: Deploy Backend Code and Restart Docker
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} << 'EOF'
            set -e

            cd ~/Learning-Application-Server

            echo "🔄 Pulling latest code..."
            git pull origin development

            echo "🐳 Restarting Docker..."
            docker-compose down -v
            docker-compose up --build -d

            echo "✅ Deployment complete."
          EOF
